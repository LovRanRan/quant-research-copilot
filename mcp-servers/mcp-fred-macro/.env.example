# FRED macro MCP — required env
FRED_API_KEY=your-fred-api-key-here

# Optional: override cache dir (default = ./.cache/fred)
# FRED_CACHE_DIR=./.cache/fred

# Optional: override rate limit (FRED allows 120/min; leave blank to use default)
# FRED_RATE_LIMIT_PER_MIN=120